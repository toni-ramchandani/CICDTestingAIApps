name: Giskard-CI-CD

on:
  push:
    paths:
      - 'data/**'
      - 'model/**'
      - 'giskard_validation.py'
      - 'README.md'
      
    branches:
      - main
      - feature

permissions:
  contents: read

jobs:
  run-giskard-test-suite:
    name: Giskard-Test-Suite
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install pipenv

      - name: Install dependencies
        working-directory: .
        run: pipenv install --system --deploy

      - name: Execute Giskard Test Suite
        run: python giskard_validation.py

      - name: Debugging TEST_RESULT
        run: |
          echo "Raw TEST_RESULT: ${{ env.TEST_RESULT }}"
          TEST_RESULT_ESCAPED=$(echo ${{ env.TEST_RESULT }} | sed 's/\\u201c/"/g' | sed 's/\\u201d/"/g')
          echo "Escaped TEST_RESULT: $TEST_RESULT_ESCAPED"
      
      - name: PR comment
        uses: actions/github-script@v6
        with:
          script: |
            try {
              const issue_number = context.payload.pull_request ? context.payload.pull_request.number : null;
              if (issue_number) {
                if (!process.env.TEST_RESULT) {
                  console.log("No TEST_RESULT found.");
                  return;
                }
                // Decode escaped characters in TEST_RESULT
                let testResults = process.env.TEST_RESULT.replace(/\\u201c/g, '"').replace(/\\u201d/g, '"');
                try {
                  testResults = JSON.stringify(JSON.parse(testResults), null, 2);
                } catch (error) {
                  console.log("Error parsing TEST_RESULT:", error.message);
                  return;
                }
                github.rest.issues.createComment({
                  issue_number: issue_number,
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  body: `Test Suites Results:\n\n\`\`\`json\n${testResults}\n\`\`\``
                });
              } else {
                console.log("No issue number found. This may not be a pull request event.");
              }
            } catch (error) {
              console.log("Error in PR comment action:", error.message);
            }
